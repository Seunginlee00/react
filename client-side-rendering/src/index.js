import React from 'react';
import ReactDOM from 'react-dom';
import RouterWrapper from './router-wrapper';

//routerwapper 컴포넌트 안에 app.js가 존재합니다.
ReactDOM.render(<RouterWrapper />, document.getElementById('root'));
